<%= form_for([note, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h3>Drop your comment</h3>
    <%= f.text_area :body, :required => true, :class => 'enlarge_width jqTranarea', :style => 'height:150px' %>
  </div>

  <div class="actions comment_actions">
    <button class="sexybutton sexyteal sexysimple sexylarge">Submit</button> <%= link_to 'Cancel', notes_path, :class => 'sexybutton sexyred sexysimple sexylarge' %>
  </div>

<% end %>

<script>
$(function(){
      $('form').jqTransform({imgPath:'jqtransformplugin/img/'});
    });
</script>