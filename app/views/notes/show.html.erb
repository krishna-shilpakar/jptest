<p id="notice"><%= notice %></p>

<section class="hfeed">

  <article class="hentry">
    <hgroup>
      <h2 class="entry-title"><a href="#"><%= @note.title %></a></h2>
      <h6>Posted by <a class="author" href="#">Guest</a> on <abbr class="updated published" title="20100228T15:08:00"><%= @note.created_at.strftime("%F")%></a></time></h6>
    </hgroup>
    <p class="entry-summary"><%= @note.body %></p>
    <footer><a href="#"><%= pluralize(@note.comments.count, 'comment')%></a> &emsp;&bull;&emsp; <span = 'under_category'><%= @note.category.try(:name) %></span> </footer>
    <br /><hr /><br />
  </article>
  <%= render "comments/form", :note =>  @note %>
  <%#= link_to 'Edit', edit_note_path(@note), :class => 'sexybutton sexyblue sexysimple' %>
  <%#= link_to 'Back', notes_path, :class => 'sexybutton sexymagenta sexysimple' %> <%#= link_to 'New Note', new_note_path, :class => 'sexybutton sexyteal sexysimple' %>
</section>


